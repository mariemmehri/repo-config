apiVersion: v1
kind: Service
metadata:
  name: hr-backend
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: hr-backend
  ports:
    - protocol: TCP
      port: 8081
      targetPort: {{ .Values.backend.port }}