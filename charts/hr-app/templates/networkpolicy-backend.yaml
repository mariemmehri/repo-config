{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: hr-backend
  namespace: {{ .Release.Namespace }}
spec:
  podSelector:
    matchLabels:
      app: hr-backend
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: hr-frontend
      ports:
        - protocol: TCP
          port: {{ .Values.backend.port }}
  egress:
    # Dataplane V2 (Cilium) resolves the kube-dns ClusterIP to its backing
    # pods before enforcing NetworkPolicy, unlike Calico — so this selector
    # survives a cluster recreate with no hardcoded/env-specific IP to update.
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    {{- if .Values.backend.database.enabled }}
    - to:
        - podSelector:
            matchLabels:
              cnpg.io/cluster: pg-staging
      ports:
        - protocol: TCP
          port: 5432
    {{- end }}
{{- end }}
