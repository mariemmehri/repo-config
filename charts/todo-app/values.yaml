backend:
  image:
    repository: acrtodosopra.azurecr.io/todo-backend
    # Tag immuable attendu: le pipeline CI met ensuite un SHA Git par environnement.
    tag: "bootstrap-sha"
  replicas: 1
  port: 8081

frontend:
  image:
    repository: acrtodosopra.azurecr.io/todo-frontend
    # Tag immuable attendu: le pipeline CI met ensuite un SHA Git par environnement.
    tag: "bootstrap-sha"
  replicas: 1
  port: 80

ingress:
  enabled: false
  host: todo.example.com