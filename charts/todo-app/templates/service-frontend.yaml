apiVersion: v1
kind: Service
metadata:
  name: todo-frontend
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: todo-frontend
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ .Values.frontend.port }}